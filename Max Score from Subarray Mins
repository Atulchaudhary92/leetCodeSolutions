class Solution {
  public:
    int maxSum(vector<int> &arr) {
        int i=0;
        int maxSum=0;
        for(int j=1;j<arr.size();j++){
            int temp=arr[i++]+arr[j];
            maxSum=max(maxSum,temp);
            }
            
        return maxSum;
    }
};
